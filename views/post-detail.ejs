<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css">
    <link rel="icon" href="https://i.ibb.co/VDHvzFH/oss-logo-h-1.png" type="image/x-icon">
    <style>
        body {
            background-color: #f8f9fa;
            padding: 20px;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
    
        .navbar {
            background-color: #af2a2a;
            color: #fff;
            border-bottom: 1px solid #ccc;
        }

        .navbar-brand {
            color: #fff;
            font-weight: bold;
            font-size: 24px;
        }

        .navbar-brand img {
            width: 40px; 
            height: auto;
            margin-right: 10px;
            border-radius: 30px;
        }

        .back-button {
            color: #fff;
            font-size: 20px;
            margin-left: 10px;
            background: transparent;
            border: none;
            cursor: pointer;
        }

        .back-button:hover {
            color: #ccc;
        }

        h3 {
            color: #020202;
            margin-bottom: 20px;
            text-align: center;
        }

        p {
            font-size: 18px;
            color: #6c757d;
        }

        .post-image-container {
            max-width: 80%;
            height: auto;
            margin-top: 20px;
            display: block;
            margin-left: auto;
            margin-right: auto;
            overflow: hidden;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        .post-image {
            width: 100%;
            height: auto;
            border-radius: 10px;
            transition: transform 0.3s ease-in-out;
        }

        .post-image:hover {
            transform: scale(1.05);
        }

        .link {
            margin-top: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .link-icon {
            margin-right: 10px;
            font-size: 20px;
            color: #ff0000;
        }

        .link a {
            color: #ff0000;
            text-decoration: none;
            transition: color 0.3s ease-in-out;
        }

        .link a:hover {
            color: #218838;
        }

        .post-content {
            margin-top: 20px;
            font-size: 16px;
            line-height: 1.6;
            text-align: justify;
            white-space: pre-line; 
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/">
                <img src="https://i.ibb.co/VDHvzFH/oss-logo-h-1.png" alt="Logo"> 
                KAREOSS 
            </a>
            <button class="back-button" onclick="window.location.href='/'">
                <i class="fas fa-arrow-left"></i> Back
            </button>
        </div>
    </nav>
    <div class="container">
        <h3><%= post.title %></h3>
        <p>Category: <%= post.category %></p>
        <div class="post-image-container">
            <% if (post.poster && post.poster.data) { %>
                <img src="data:<%= post.poster.contentType %>;base64,<%= post.poster.data.toString('base64') %>" alt="Poster Image" class="post-image">
            <% } %>
        </div>
        <div class="link">
            <span class="link-icon"><i class="fas fa-external-link-alt"></i></span>
            <a href="<%= post.link %>" target="_blank">APPLY NOW</a>
        </div>
        <div class="post-content">
            <%= post.content %>
        </div>
    </div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/js/all.min.js"></script>
</body>
</html>
